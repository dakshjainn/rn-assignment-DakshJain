# To-Do List App

A simple React Native To-Do app built with Expo and TypeScript.

## Features

- Add tasks
- Mark tasks complete/incomplete
- Delete tasks
- Persistent storage with AsyncStorage

## How to Run

```bash
cd todo-app
npm install
npx expo start
```

Scan the QR code with Expo Go app or press `i` for iOS simulator / `a` for Android emulator.

## Dependencies

- Expo SDK 53
- TypeScript
- @react-native-async-storage/async-storage

## Folder Structure

```
/todo-app
  /src
    /components
      TaskItem.tsx
    /screens
      Home.tsx
  App.tsx
```

